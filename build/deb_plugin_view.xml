<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.plugin_view" default="build" basedir=".">
    <property name="deb.plugin_view.setup" value="${deb.setup}/plugin/view"/>
    <property name="deb.plugin_view.source" value="${basedir}/"/>
    <property name="deb.plugin_view.staging" value="${deb.staging}/plugin_view/deb"/>

    <property name="deb.plugin_view.outputfile" value="${deb.builddir}/HUGnetLab_plugin_view_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.plugin_view.staging}" failonerror="false"/>
        <delete failonerror="false">
            <fileset dir="${deb.plugin_view.builddir}" includes="*.deb"/>
        </delete>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetLab View Plugin'v${deb.version}'</echo>

        <mkdir dir="${deb.plugin_view.staging}"/>
        <copy todir="${deb.plugin_view.staging}">
            <fileset dir="${deb.plugin_view.setup}"/>
        </copy>
        <mkdir dir="${deb.plugin_view.staging}${deb.prefix}/share/php/HUGnetLab/plugins/"/>
        <copy todir="${deb.plugin_view.staging}${deb.prefix}/share/php/HUGnetLab/plugins/"
              file="${deb.plugin_view.source}/include/plugins/view.php" />
        <chmod file="${deb.plugin_view.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.plugin_view.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <chmod file="${deb.plugin_view.staging}/DEBIAN/postrm" perm="ugo+rx"/>
        <replaceregexp file="${deb.plugin_view.staging}/DEBIAN/control" byline="yes"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.plugin_view.staging}
                       ${deb.plugin_view.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_view.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_view.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_view.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.plugin_view.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
