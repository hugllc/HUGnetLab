<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.plugin_serverconfig" default="build" basedir=".">
    <property name="deb.plugin_serverconfig.setup" value="${deb.setup}/plugin/serverconfig"/>
    <property name="deb.plugin_serverconfig.source" value="${basedir}/"/>
    <property name="deb.plugin_serverconfig.staging" value="${deb.staging}/plugin_serverconfig/deb"/>

    <property name="deb.plugin_serverconfig.outputfile" value="${deb.builddir}/HUGnetLab_plugin_serverconfig_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.plugin_serverconfig.staging}" failonerror="false"/>
        <delete failonerror="false">
            <fileset dir="${deb.plugin_serverconfig.builddir}" includes="*.deb"/>
        </delete>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetLab Server Config Plugin'v${deb.version}'</echo>

        <mkdir dir="${deb.plugin_serverconfig.staging}"/>
        <copy todir="${deb.plugin_serverconfig.staging}">
            <fileset dir="${deb.plugin_serverconfig.setup}"/>
        </copy>
        <mkdir dir="${deb.plugin_serverconfig.staging}${deb.prefix}/share/php/HUGnetLab/plugins/"/>
        <copy todir="${deb.plugin_serverconfig.staging}${deb.prefix}/share/php/HUGnetLab/plugins/"
              file="${deb.plugin_serverconfig.source}/plugins/serverconfig.php" />
        <chmod file="${deb.plugin_serverconfig.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.plugin_serverconfig.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <chmod file="${deb.plugin_serverconfig.staging}/DEBIAN/postrm" perm="ugo+rx"/>
        <replaceregexp file="${deb.plugin_serverconfig.staging}/DEBIAN/control" byline="yes"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.plugin_serverconfig.staging}
                       ${deb.plugin_serverconfig.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_serverconfig.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_serverconfig.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_serverconfig.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.plugin_serverconfig.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
