<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.lab" default="build" basedir=".">
    <property name="deb.lab.setup" value="${deb.setup}/lab"/>
    <property name="deb.lab.source" value="${basedir}/lab"/>
    <property name="deb.lab.staging" value="${deb.staging}/lab/deb"/>

    <property name="deb.lab.outputfile" value="${deb.builddir}/HUGnetLab_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.lab.staging}" failonerror="false"/>
        <delete failonerror="false">
            <fileset dir="${deb.lab.builddir}" includes="*.deb"/>
        </delete>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetLab 'v${deb.version}'</echo>

        <mkdir dir="${deb.lab.staging}"/>
        <copy todir="${deb.lab.staging}">
            <fileset dir="${deb.lab.setup}"/>
        </copy>
        <chmod file="${deb.lab.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.lab.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <chmod file="${deb.lab.staging}/DEBIAN/postrm" perm="ugo+rx"/>
        <chmod file="${deb.lab.staging}/DEBIAN/config" perm="ugo+rx"/>
        <replaceregexp file="${deb.lab.staging}/DEBIAN/control" byline="yes"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.lab.staging}
                       ${deb.lab.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.lab.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.lab.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.lab.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.lab.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
