<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.plugin_config" default="build" basedir=".">
    <property name="deb.plugin_config.setup" value="${deb.setup}/plugin/config"/>
    <property name="deb.plugin_config.source" value="${basedir}/"/>
    <property name="deb.plugin_config.staging" value="${deb.staging}/plugin_config/deb"/>

    <property name="deb.plugin_config.outputfile" value="${deb.builddir}/HUGnetLab_plugin_config_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.plugin_config.staging}" failonerror="false"/>
        <delete failonerror="false">
            <fileset dir="${deb.plugin_config.builddir}" includes="*.deb"/>
        </delete>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetLab Config Plugin'v${deb.version}'</echo>

        <mkdir dir="${deb.plugin_config.staging}"/>
        <copy todir="${deb.plugin_config.staging}">
            <fileset dir="${deb.plugin_config.setup}"/>
        </copy>
        <mkdir dir="${deb.plugin_config.staging}${deb.prefix}/share/php/HUGnetLab/plugins/"/>
        <copy todir="${deb.plugin_config.staging}${deb.prefix}/share/php/HUGnetLab/plugins/"
              file="${deb.plugin_config.source}/include/plugins/config.php" />
        <chmod file="${deb.plugin_config.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.plugin_config.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <chmod file="${deb.plugin_config.staging}/DEBIAN/postrm" perm="ugo+rx"/>
        <replaceregexp file="${deb.plugin_config.staging}/DEBIAN/control" byline="yes"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.plugin_config.staging}
                       ${deb.plugin_config.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_config.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_config.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.plugin_config.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.plugin_config.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
